DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=search_images_engine_db
PORT=3000
TRANSFORMERS_CACHE=./.cache
JWT_SECRET_PEXELS=QU7YdjpwgUTSnXlUxHY1wsq4jL0gD1qGcERlNgd58sXmoOcca1uU0VSG

SIM_WORKERS=4 
ONNX_NUM_THREADS=4                   
EMBED_CONCURRENCY=2

# Augmentation safety knobs
# Disable OpenCV-based photometric augmentations (use geometric-only)
AUGMENT_ENABLE_CV=true
# Skip CV augment if image exceeds this many pixels (width*height)
AUGMENT_MAX_PIXELS=3200000

# Robust rerank parameters (for tiny-crop handling)
# Grid size per side (e.g., 7 => 7x7)
ROBUST_GRID=7
# Overlap ratio between tiles (0..0.9)
ROBUST_OVERLAP=0.5

# Visual style rerank (shape/color)
STYLE_RERANK=true
STYLE_RERANK_K=20
# Weights for combined scoring (override only if needed)
# FEAT_W_CLIP=0.85
# FEAT_W_SHAPE=0.10
# FEAT_W_COLOR=0.05

# Feature cache size
FEATURE_CACHE_MAX=500
