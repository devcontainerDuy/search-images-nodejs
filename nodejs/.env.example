DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=image_search_db
PORT=3000

JWT_SECRET_PEXELS=

# Robust rerank parameters (for tiny-crop handling)
# Grid size per side (e.g., 7 => 7x7)
ROBUST_GRID=7
# Overlap ratio between tiles (0..0.9)
ROBUST_OVERLAP=0.5
